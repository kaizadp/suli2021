# 2021-06-02
# Tutorial

## 1. import the `iris.csv` dataset (from the `datasets` folder)
## 2. find the column names
## 3. find the dataset structure. how many rows/columns? what column types?
## 4. make an exploratory plot of the `iris` dataset using plot(). look for relationships among the different variables
## 5. install and load the {dplyr} package
## 6. rename all the columns in the `iris` dataset, replacing all the periods (.) with underscores (_)
##    i.e., "Sepal_Length", "Sepal_Width", etc.